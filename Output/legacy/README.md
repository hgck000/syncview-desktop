# Legacy build

`DO-NOT-USE-SyncView-0.0.0-Setup.exe` is kept only to preserve repository
history. It predates the verified release pipeline and must not be distributed.

From the parent `Output` directory, run `Install-SyncView.cmd` to download and
launch the latest verified Windows installer.
